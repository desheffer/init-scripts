---

- name: Install base packages
  pacman:
    name: "{{ item }}"
    update_cache: yes
  with_items:
    - base-devel
    - bash
    - bash-completion
    - bc
    - coreutils
    - git
    - jq
    - rsync
    - the_silver_searcher
    - tmux
    - tree
    - vim
    - yay

- include: user.yml
