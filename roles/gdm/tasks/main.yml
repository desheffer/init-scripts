---

- name: Install GDM
  pacman:
    name: "{{ item }}"
    update_cache: yes
  with_items:
    - gdm

- name: Enable GDM
  service:
    name: gdm
    enabled: yes

- name: Copy dconf gdm profile
  template:
    src: gdm
    dest: /etc/dconf/profile/gdm
