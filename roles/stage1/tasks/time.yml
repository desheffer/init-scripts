---

- name: Set time zone
  file:
    src: "/usr/share/zoneinfo/{{ timezone }}"
    dest: /etc/localtime
    state: link

- name: Set system clock
  command: "hwclock --systohc --utc"
  args:
    creates: /etc/adjtime
