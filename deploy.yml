---

- hosts: localhost
  become: yes
  roles:
    - { role: base, tags: ['base'] }
    - { role: gdm, tags: ['gdm'] }
    - { role: gnome, tags: ['gnome'] }
    - { role: extra, tags: ['extra'] }
    - { role: dotfiles, tags: ['dotfiles'] }
    - { role: docker, tags: ['docker'] }
